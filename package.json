{
  "name": "Climate-Walker",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && npx gh-pages --dotfiles -d .output/public"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "daisyui": "^5.1.10",
    "nuxt": "^4.1.1",
    "nuxt-aos": "^1.2.5",
    "nuxt-swiper": "^2.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
